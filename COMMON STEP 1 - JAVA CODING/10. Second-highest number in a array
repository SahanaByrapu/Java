public int secondHighestNumber(int[] nums)
{

    int secondLargest=0, Largest=0,difference=0;
    for(int i=0;i<nums.length;i++)
    {
        if(nums[i]>Largest)
        {
            secondLargest=Largest;
            Largest=nums[i];
        }
        else if(secondLargest<nums[i] && nums[i]!=)Largest
        {
            secondLargest=num[i];
        }

    }

    return secondLargest;

}